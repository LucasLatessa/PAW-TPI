{% extends 'layouts/app.view.twig' %}

{% block styles %}
<link rel="stylesheet" href="/assets/styles/noticias/show.css" />
{% endblock %}

{% block content %}
    <article class="noticia-detalle">

        <header class="noticia-header">
            <h1>{{ noticia.titulo }}</h1>

            <div class="meta">
                {% if noticia.fecha_publicacion %}
                    <time datetime="{{ noticia.fecha_publicacion.format('Y-m-d') }}">
                        {{ noticia.fecha_publicacion.format('d M Y') }}
                    </time>
                {% endif %}

                {% if noticia.autor %}
                    <span class="autor">Por: {{ noticia.autor }}</span>
                {% endif %}

                <span class="visitas">
                    {{ noticia.visitas }} visitas
                </span>
            </div>
        </header>

        {% if noticia.imagen %}
            <figure class="noticia-imagen-principal">
                <img src="/assets/{{noticia.imagen}}" alt="{{ noticia.titulo }}">
            </figure>
        {% endif %}


        <section class="noticia-contenido">
            {{ noticia.contenido }}
        </section>

        <footer class="noticia-footer">
            <a href="/noticias" class="volver">
                ← Volver a noticias
            </a>
        </footer>

    </article>

{% endblock %}